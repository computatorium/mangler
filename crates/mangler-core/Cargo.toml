[package]
name = "mangler-core"
version = "0.1.0"
edition = "2024"
description = "Dependency-free foundation for mangler: per-pass RNG, name allocation, hashing, errors, diagnostics, and the Language trait."

[dependencies]
thiserror = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }
